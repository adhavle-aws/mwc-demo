# ============================================================================
# Agent UI Production Environment Variables
# ============================================================================
# 
# This file contains production environment configuration.
# These values are used when building for production deployment.
#
# IMPORTANT: 
# - Update VITE_API_BASE_URL with your deployed API Gateway URL
# - Get the URL from: aws cloudformation describe-stacks --stack-name agent-ui-api
# - Or from the deployment output after running: cd api && ./deploy.sh
#
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration - PRODUCTION
# ----------------------------------------------------------------------------

# Backend API URL from Lambda Function URL (streaming support, no timeout)
# Format: https://{function-id}.lambda-url.{region}.on.aws/
VITE_API_BASE_URL=https://led3hq4drjye4du3fcf2ldxjiq0uacqx.lambda-url.us-east-1.on.aws

# ----------------------------------------------------------------------------
# Build Configuration
# ----------------------------------------------------------------------------

# Node environment (automatically set by Vite)
# NODE_ENV=production

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
#
# 1. This file is used during production builds (npm run build)
# 2. Update VITE_API_BASE_URL after deploying the backend API
# 3. The API URL is embedded at build time, not runtime
# 4. Redeploy the frontend after updating this file
#

