version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd agent-ui
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: agent-ui/dist
    files:
      - '**/*'
  cache:
    paths:
      - agent-ui/node_modules/**/*
